/**
 * Shared attempt session helpers (used by instructions + taking flows).
 */
export {
  clearAttemptSession,
  getAttemptSession,
  normalizeAttemptId,
  setAttemptSession,
} from '../../test-instructions/utils/attemptSession.js';
