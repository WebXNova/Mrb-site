export default function AdminSettingsPage() {
  return (
    <section className="admin-card">
      <h2 className="heading-3">Settings</h2>
      <p className="body-md" style={{ marginTop: '0.75rem' }}>
        Settings panel shell is ready. System-level settings (security, integrations, and
        notifications) can be connected in the next admin iteration.
      </p>
    </section>
  );
}
