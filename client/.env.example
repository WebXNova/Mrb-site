# API base URL (optional — defaults to same-origin /api in production builds)
# VITE_API_BASE_URL=http://localhost:4000/api

# Google Sign-In — same OAuth 2.0 Web Client ID as server GOOGLE_CLIENT_ID
# Authorized JavaScript origins must include this Vite dev server / production URL.
VITE_GOOGLE_CLIENT_ID=
