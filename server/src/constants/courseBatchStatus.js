/** Canonical lifecycle values for `course_batches.status` (lowercase) — simplified to 3 states. */
export const COURSE_BATCH_STATUSES = [
  'draft',
  'published',
  'archived',
];

/** Statuses exposed on the public catalog batch list (active operational cohorts). */
export const COURSE_BATCH_PUBLIC_STATUSES = ['published'];
