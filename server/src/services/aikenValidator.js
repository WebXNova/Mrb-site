/**
 * Aiken question validation for question bank import (Phase 2.1 stub).
 */

export function validateAikenQuestions(questions) {
  void questions;
  return {
    validQuestions: [],
    invalidQuestions: [],
  };
}
